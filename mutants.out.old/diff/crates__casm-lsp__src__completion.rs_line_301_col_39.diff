--- crates/casm-lsp/src/completion.rs
+++ replace <= with > in enclosing_pattern
@@ -293,17 +293,17 @@
     index: &DocumentIndex,
     patterns: &'a [Pattern],
     line: u32,
 ) -> Option<&'a Pattern> {
     let reference = index
         .claims()
         .iter()
         .rev()
-        .find(|claim| claim.item_line <= line)?
+        .find(|claim| claim.item_line > /* ~ changed by cargo-mutants ~ */ line)?
         .reference
         .as_deref()?;
 
     patterns
         .iter()
         .find(|pattern| pattern.reference() == reference)
 }
 
