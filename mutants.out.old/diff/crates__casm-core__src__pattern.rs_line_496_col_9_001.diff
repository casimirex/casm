--- crates/casm-core/src/pattern.rs
+++ replace Pattern::metadata -> &BTreeMap<String, String> with Box::leak(Box::new(BTreeMap::from_iter([(String::new(), String::new())])))
@@ -488,17 +488,17 @@
     pub fn satisfies(&self) -> &[String] {
         &self.satisfies
     }
 
     /// The pattern's annotations.
     #[inline]
     #[must_use]
     pub const fn metadata(&self) -> &BTreeMap<String, String> {
-        &self.metadata
+        Box::leak(Box::new(BTreeMap::from_iter([(String::new(), String::new())]))) /* ~ changed by cargo-mutants ~ */
     }
 
     /// Looks up a requirement by role name.
     #[must_use]
     pub fn requirement(&self, role: &str) -> Option<&Requirement> {
         self.requirements.iter().find(|r| r.role().as_str() == role)
     }
 
