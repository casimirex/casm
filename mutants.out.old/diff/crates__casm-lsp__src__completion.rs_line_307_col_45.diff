--- crates/casm-lsp/src/completion.rs
+++ replace == with != in enclosing_pattern
@@ -299,17 +299,17 @@
         .iter()
         .rev()
         .find(|claim| claim.item_line <= line)?
         .reference
         .as_deref()?;
 
     patterns
         .iter()
-        .find(|pattern| pattern.reference() == reference)
+        .find(|pattern| pattern.reference() != /* ~ changed by cargo-mutants ~ */ reference)
 }
 
 /// Completions for a position that is writing a value.
 fn value_completions(
     index: &DocumentIndex,
     patterns: &[Pattern],
     section: Section,
     block: Block,
